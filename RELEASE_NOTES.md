# XHS-FB 0.4.43

XHS-FB 0.4.43 增加安装版更新通道，并把安装版与便携版的更新清单升级为独立域的 Ed25519 签名协议。日常使用推荐安装版；便携版继续作为无需安装的完整交付。

## 应用内签名更新

- “设置 > 软件更新”可检查 GitHub 最新稳定版、在后台下载，并由用户确认后退出安装。
- 安装版读取 `installer-update-manifest.json`，便携版读取 `release-manifest.json`；两个清单使用不同签名域，不能互相冒充。
- 程序在请求或运行大文件之前先验证 Ed25519 清单签名，再校验版本、文件名、大小和 SHA-256。
- 更新公钥固定内置在程序中，发布私钥不进入源码、成品包、公开仓库或日志。
- 发布任务、图片生成任务或浏览器登录仍在运行时，更新安装会被安全拦截，不会强制中断工作。
- 外置更新助手只更新当前已验证的程序目录；新版首屏健康检查失败时会恢复旧版并重新打开。

## 安装版与便携版

- 正式 Release 同时提供 Windows x64 安装程序和包含全部运行组件的便携 ZIP。
- 安装版注册当前用户的安装位置，便于后续原位升级；任务、账号、素材、AI 设置和专用浏览器 Profile 继续存放在独立数据目录。
- 便携 ZIP 内包含 Electron/Node.js、Playwright 自动化组件和专用 `ungoogled-chromium-windows` 浏览器，完整解压后可直接运行。
- 每个正式版本固定为六个资产：安装包、便携包、两个 SHA-256 文件和两个签名清单。
- 仓库根目录的 `release-manifest.json` 必须与同版本 Release 中的便携清单逐字节一致。

## 从旧版本升级

`0.4.41` 及更早版本没有新的签名清单验证能力。请退出旧版后，手动运行 `XHS-Publisher-0.4.43-x64.exe` 并覆盖原安装位置一次；不要先卸载。完成这次引导升级后，`0.4.43` 及后续稳定版可以使用应用内签名更新。

## 安全与隐私

- Ed25519 签名用于确认更新清单来自受信发布者；SHA-256 用于确认下载的大文件与清单一致。
- Ed25519 清单签名不等于 Windows Authenticode 代码签名。当前版本仍可能触发 SmartScreen 来源提示。
- 公开仓库、安装包和便携包不包含 Cookie、浏览器 Profile、API Key、任务数据库、草稿、日志、商品图片、个人素材或更新签名私钥。

## 发布校验

GitHub 发布门禁会拒绝多余、缺失、重复或未完成上传的 Release 资产，并验证两个清单的严格 JSON 结构、无重复键、Ed25519 签名、文件大小、SHA-256、便携目录结构和隐私边界。本版已通过 83 个测试文件、871 项测试、桌面构建、安装版原位升级、数据与快捷方式连续性、便携包完整性、更新助手故障恢复、全包脱敏、持久 Profile、文件上传和干净首屏验证。

---

## XHS-FB 0.4.41

XHS-FB 0.4.41 为素材盘补充了明确的“未通过素材删除”流程，并继续保留 0.4.40 引入的应用内安全更新能力。

### 素材盘

- 在“待处理图片”的批量操作区增加“删除选择素材”。
- 删除前显示二次确认；成功后清空选择并更新待处理数量，失败时保留原选择。
- 删除入口只服务于未通过素材；已经归类到款号文件夹的过关素材不显示该入口。
- 后端会整批拦截已归类、正在发布、已发布或无效的素材，避免部分删除或误删。
- 删除不会改动发布任务、草稿、Vela 原文件或本机媒体副本；本机移除标记可防止同一未通过素材再次同步出现。

### 应用内更新

- 在“设置 > 软件更新”中检查 GitHub 最新稳定版。
- 更新包可在后台下载，并实时显示字节进度。
- 安装前同时核对 Release、发布清单、文件名、大小与 SHA-256。
- 发布任务或图片生成仍在运行时不会退出安装。
- 用户主动确认后，外置更新助手会原位切换程序并重新打开新版。
- 新版健康检查失败时自动恢复并重新打开旧版。
- 任务、账号、素材、AI 设置和专用浏览器 Profile 保留在独立数据目录，不随程序目录替换。

从 `0.4.40` 起，后续稳定版可在软件内完成更新。

### 便携交付

- ZIP 内已包含程序、Electron/Node.js 运行组件、发布自动化组件和专用浏览器。
- 另一台电脑完整解压后可直接双击 `XHS-FB.exe`，无需安装 Node.js、npm、Electron、Playwright 或系统 Chrome。
- 内置 `ungoogled-chromium-windows` 151.0.7922.71-1.1 x64。

### 隐私边界

公开便携包不包含开发电脑的 Cookie、浏览器 Profile、API Key、任务数据库、草稿、日志、商品图片或个人素材。新电脑首次使用仍需本人登录账号并按需配置服务。

### 下载校验

发布页同时提供 ZIP、同名 `.sha256` 和 `release-manifest.json`。三处记录的版本、文件名、文件大小和 SHA-256 必须一致。

本版正式包已经过 79 个测试文件、837 项测试、桌面构建、更新助手故障恢复测试、全包脱敏、持久 Profile、文件上传和干净首屏验证。

### 已知提示

当前版本未承诺代码签名，Windows SmartScreen 可能出现来源确认提示。请先核对发布来源和 SHA-256，再决定是否继续运行。
